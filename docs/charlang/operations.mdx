---
sidebar_position: 4
---

# 운영과 유지보수

## 로컬 실행

```bash
npm install
npm run hooks:install
npm run dev
```

개발 서버는 Granite가 실행해요.
보통 `http://localhost:5173/`에서 확인해요.

## 검증

PR을 열기 전 최소 검증은 다음이에요.

```bash
npx tsc --noEmit
npm run lint
npm run build
git diff --check
```

Apps in Toss SDK를 쓰는 기능은 일반 브라우저에서 fallback도 확인해야 해요.
광고, 햅틱, Safe Area, 배포는 실제 Apps in Toss 환경에서 한 번 더 확인해야 해요.
IAP 기능은 브라우저에서는 bridge 미지원 실패 안내까지만 확인할 수 있으므로, 구매, 지급 완료, 미결 주문 복원, 환불 회수는 Apps in Toss sandbox나 실제 지원 환경에서 별도로 확인해야 해요.
Apps in Toss 이벤트 로그는 샌드박스와 QR 테스트 환경에서는 실제로 집계되지 않아요.
라이브 출시 후 다음 날부터 콘솔의 분석 > 이벤트 화면에서 수집 여부를 확인해야 해요.
Analytics 보강 작업에서는 `npm run lint`와 `npm run build`를 최소 검증으로 통과했어요.
빌드 중 기존 Vite 동적/정적 import 경고와 Node `DEP0190` 경고가 보일 수 있지만, 명령이 성공하면 문서상 실패로 보지 않아요.

로그를 점검할 때는 다음 흐름을 우선 확인해요.

- 시간 설정 다이얼로그에서 값을 바꾼 뒤 닫으면 `timer_duration_changed`가 한 번만 남는지 확인해요.
- 시작 버튼을 누르면 기존 기본 클릭 로그와 별도로 `timer_start_click`이 현재 구간, 진행 모드, 라운드를 담는지 확인해요.
- 자동/수동 진행과 볼륨 변경은 변경 전후 값이 함께 들어가는지 확인해요.
- PRO 다이얼로그를 결제 시도 없이 닫으면 `pro_dialog_dismissed_without_purchase`가 닫힘 방식과 함께 남는지 확인해요.
- 차트 페이지에 들어가면 `chart_header_impression`이 화면 헤더 문구와 완료 사이클 문구를 같은 값으로 남기는지 확인해요.

## 빌드와 배포

```bash
npm run build
npm run deploy
```

`npm run build`는 Apps in Toss 번들을 만들어요.
GitHub Actions는 `master` 또는 `main`에 push되면 lint, build, artifact upload, Apps in Toss bundle upload를 실행해요.

자동 업로드에는 GitHub Actions secret `APPS_IN_TOSS_API_KEY`가 필요해요.
secret 값은 문서, 코드, 로그에 남기지 않아요.
자동화는 번들 업로드까지 담당하고, 심사 제출과 실제 출시는 Apps in Toss 콘솔에서 확인하는 흐름으로 봐야 해요.

## 환경변수

광고 그룹 ID는 env 파일이나 배포 환경변수로 관리해요.
로컬 `.env`는 git에 올리지 않아요.

필요한 값은 다음과 같아요.

- `VITE_TIMER_BANNER_AD_GROUP_ID`: 타이머 화면 배너 광고 그룹 ID
- `VITE_CHART_BANNER_AD_GROUP_ID`: 차트 화면 배너 광고 그룹 ID

전면형 광고 그룹 ID도 출시 전 실제 값으로 교체해야 해요.
문서, PR 본문, 로그에는 실제 secret이나 민감한 키 값을 직접 남기지 않아요.

PRO 광고 제거 상품은 Apps in Toss 콘솔의 IAP 상품 설정과 코드의 SKU가 일치해야 해요.
SKU 자체는 결제 상품 식별자이지만, API key나 secret처럼 문서에 불필요하게 반복 노출하지 않아요.

## 브랜치와 PR 규칙

`master`와 `main`에 직접 커밋하거나 직접 푸시하지 않도록 git hook을 설치해요.

```bash
npm run hooks:install
```

작업 순서는 다음을 기본으로 해요.

1. `master`를 최신화해요.
2. 기능 브랜치를 만들어요.
3. 변경하고 검증해요.
4. 한국어 PR을 작성해요.
5. PR 병합 뒤 `master`를 다시 최신화해요.

PR 본문은 파일 경로를 나열하지 않고, 처음 개발을 시작한 사람도 이해할 수 있는 난이도로 적어요.
문체는 Toss 앱의 결에 맞춰 `-요` 체를 사용해요.

## Safe Area와 WebView

하단 토스트와 조작 버튼은 safe area를 고려해야 해요.
Apps in Toss 환경에서는 `SafeAreaInsets.get()`과 `SafeAreaInsets.subscribe()`를 우선 사용하고, 일반 브라우저에서는 CSS `env(safe-area-inset-bottom)` fallback을 사용해요.

홈 화면은 기본적으로 viewport 안에 들어오도록 설계해요.
다만 아주 작은 해상도에서는 세로 스크롤이 필요할 수 있으므로 완전 차단하지 않고, 오버스크롤 고무줄 효과만 억제해요.

## 출시 전 점검

- Apps in Toss 콘솔의 앱 등록명과 `granite.config.ts` 표시 이름이 일치하는지 확인해요.
- 타이머 배너, 차트 배너, 전면형 광고 그룹 ID가 실제 운영 값으로 연결되어 있는지 확인해요.
- Apps in Toss IAP 상품이 판매 가능 상태이고, PRO SKU가 코드와 콘솔에서 일치하는지 확인해요.
- PRO 구매 성공 직후 배너와 휴식 전 광고가 새로고침 없이 사라지는지 확인해요.
- 미결 주문 복원과 완료/환불 주문 보정이 실제 Apps in Toss 환경에서 동작하는지 확인해요.
- 라이브 출시 후 콘솔 분석 화면에서 튜토리얼, 타이머 조작, PRO 구매 퍼널 이벤트가 들어오는지 확인해요.
- 앱 내부 커스텀 뒤로가기 버튼이 없는지 확인해요.
- input 포커스 확대를 만드는 UI가 없는지 확인해요.
- iOS safe area와 Android navigation area에서 조작 버튼과 토스트가 겹치지 않는지 확인해요.
- 실제 기기에서 AudioContext unlock, 틱톡 루프, 종료음, 햅틱을 확인해요.

## 백로그

- 화이트노이즈나 배경음 재생 시스템
- 세션별 틱톡음 5종 선택 UI
- 세션별 종료음 선택 UI
- Toss 유저 식별키 기반 동기화 저장
- 실제 광고 그룹 ID 교체와 수익화 설정
- Apps in Toss sandbox에서 PRO IAP 결제, 지급 완료, 환불 회수 최종 확인
- 출시 심사 이후 운영 지표 확인
